DISCORD_TOKEN=
DISCORD_ID=888488557352747020
DISCORD_SECRET=

# Manager Database
DB_HOST=127.0.0.1
DB_USER=activityrank
DB_PASS=
DB_NAME=manager

# Manager API
MANAGER_HOST=127.0.0.1:3000
MANAGER_AUTH=

ORIGIN=http://localhost:3000
PUBLIC_ORIGIN=http://localhost:3000

# NOTES:

# 1. PUBLIC_COMMIT_HASH and PUBLIC_APP_VERSION are also available 
# (either provided as Docker build args or generated in `vite.config.ts`).

# 2. docker-compose.yml overrides `DB_HOST` and `MANAGER_HOST` to reasonable defaults;
# use the `-e` flag when running a container to switch them back if necessary.
